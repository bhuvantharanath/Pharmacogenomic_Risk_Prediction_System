<?xml version="1.0" encoding="utf-8"?>
<!--
  PharmaGuard network security policy.

  Default: cleartext HTTP is refused everywhere. The deployed API is HTTPS, and
  an upload can carry a person's genotype data, so a downgrade must not be
  possible.

  Exception: loopback addresses, so `flutter run` against a local uvicorn keeps
  working. Traffic to 127.0.0.1 / localhost / 10.0.2.2 (the Android emulator's
  alias for the host machine) never leaves the device, so there is no network
  path for an attacker to sit on.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
