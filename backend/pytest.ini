[pytest]
testpaths = tests
# Tests import `app.*`; conftest.py also inserts this on sys.path so pytest can
# be run from either the repo root or backend/.
pythonpath = .
addopts = -q --strict-markers
